void registerHiveAdapters() {
  // Hive type adapters will be registered here after hive_generator codegen.
  // For Sprint 1, persistence is handled via shared_preferences.
  // To add Hive adapters:
  // 1. Add hive_generator and build_runner to dev_dependencies
  // 2. Add @HiveType()/@HiveField() annotations to models
  // 3. Run: dart run build_runner build
  // 4. Register generated adapters here
}
